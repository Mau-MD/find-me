import { Button } from "@mantine/core";
import { NextPage } from "next";
import { checkout } from "../utils/get-stripe";

const DonatePage: NextPage = () => {
  const routeChange = () => {
    let path = "";
  };
  return <Button onClick={() => {}}>Donate</Button>;
};

export default DonatePage;
